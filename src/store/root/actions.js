import { rootSlice } from "./slice";

export const {
  handleSetCategorys,
  handleSetProducts,
  handleAddProduct,
  handleSelectAdictional,
  handleResetCart,
  handleIncrementProduct,
  handleDecrementProduct,
} = rootSlice.actions;
